# ============================================
# Environment Variables لـ Render - SmartJudi2
# ============================================
# 
# انسخ هذه المتغيرات وأضفها في Render Dashboard:
# Dashboard → smartjudi → Environment → Add Environment Variable
#
# ============================================

# ========== Django الأساسية (مطلوبة) ==========
DJANGO_SETTINGS_MODULE=smartju.settings.production
SECRET_KEY=zqbnx@=a!8_ed&guaox$s-!4-c$0f*&5(*#mnmi)ixqae!iv^p
ALLOWED_HOSTS=smartjudi-nls1.onrender.com,*.onrender.com

# ========== Database ==========
# ملاحظة: DATABASE_URL يتم إضافتها تلقائياً عند ربط Database
# لا حاجة لإضافتها يدوياً

# ========== AI Assistant - Groq (مطلوبة للـ AI) ==========
GROQ_API_KEY=your_groq_api_key_here
GROQ_MODEL_NAME=qwen2.5-7b-instruct

# ========== RAG Engine (اختياري) ==========
# إذا كان لديك RAG Engine على HuggingFace Spaces:
# RAG_API_URL=https://your-rag-space.hf.space

# ========== Superuser (لإنشاء Admin تلقائياً) ==========
SUPERUSER_USERNAME=admin
SUPERUSER_EMAIL=admin@smartjudi.local
SUPERUSER_PASSWORD=admin123

# ========== CORS (اختياري) ==========
# إذا كان لديك Flutter App على نطاق محدد:
# CORS_ALLOWED_ORIGINS=https://your-flutter-app.com,https://another-domain.com

# ============================================
# ملاحظات:
# ============================================
# 1. SECRET_KEY: يمكنك استخدام القيمة أعلاه أو إنشاء واحدة جديدة
# 2. GROQ_API_KEY: استبدل بمفتاحك الحقيقي من https://console.groq.com/
# 3. ALLOWED_HOSTS: تأكد من تحديثها إذا غيرت اسم الخدمة
# 4. DATABASE_URL: لا تضيفها يدوياً، سيتم إضافتها تلقائياً عند ربط Database
